# Feature Files

Feature files describe KissMVC behavior in plain text.

They define the project’s behavioral contract and are written so humans can read them easily.

Scenario IDs must stay stable because PHPUnit acceptance tests will later reference them directly.
